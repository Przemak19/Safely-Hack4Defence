# Hack4Defence
# Instrukcja do uruchomienia aplikacji
W głównym folderze wykonać: 
docker compose up --build

Następnie w katalogu FE wykonać:
1. npm install
2. npm run dev

Część operatorska jest dostępna pod url:
http://localhost:5173/auth
